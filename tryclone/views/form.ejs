<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>แบบฟอร์มบันทึกข้อมูล</title>
        <link rel="stylesheet" href="css/bootstrap.min.css"/>
        <script src="js/jquery-3.4.1.slim.min.js"></script>
        <script src="js/popper.min.js" ></script>
        <scrip src="js/bootstrap.min.js" />
        <link rel="stylesheet" href="css/all.css" />
    </head>
    <body>
        <nav class="navbar navbar-expand-md navbar-light bg-dark">
            <a href="/" class="navbar-brand"><img src="images/home/logo.png"></a>
            <ul class="navbar-nav">
              <li >
                <a class="nav-link text-light text-uppercase font-weight-bold px-3 dropdown-toggle" href="/manage">จัดการสินค้า</a>            </li>
            </ul>
        </nav>
        <div class="container">
            <div class="row my-3">
                <div class="col-md-8 mb-4">
                    <!-- set method and action to send data to server -->
                    <!-- use POST instead GET to hide data -->
                    <!-- set enctype to read file when use image to send to database -->
                    <form class="form-horizontal" method="POST" action="/insert" enctype="multipart/form-data">
                        <div class="form-group">
                          <label class="control-label col-sm-2">ชื่อสินค้า</label>
                          <div class="col-sm-10">
                            <!-- set value of data -->
                            <input type="text" class="form-control" name="name">
                          </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label col-sm-2">ราคา</label>
                            <div class="col-sm-10">
                              <!-- set value of data -->
                              <input type="text" class="form-control" name="price">
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label col-sm-2">ภาพสินค้า</label>
                            <div class="col-sm-10">
                              <!-- set value of data -->

                              <input type="file" class="form-control" name="image">
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label col-sm-2">รายละเอียด</label>
                            <div class="col-sm-10">
                                <!-- set value of data -->
                                <textarea class="form-control" cols="30" rows="4" name="description"></textarea>
                            </div>
                        </div>
                        <div class="form-group">
                          <div class="col-sm-offset-2 col-sm-10">
                            <button type="submit" class="btn btn-primary">บันทึก</button>
                          </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </body>
</html>
